# AGENTS.md — design

Empieza por [catalog.md](catalog.md). El preview vivo es `kit/` (HTML). Eso no es `apps/web`. Una pantalla de producto = una fila en `screens.md` = una page el día que se implemente, copiando el kit.

Al **iterar** puerta o auth: `01-principios.md` (puerta pública) + el HTML de esa pantalla. El arquetipo “sin aside” no gana si `/` ya tiene atmósfera.

Al agregar un patrón o token: archivo o sección + **fila en el catálogo**.
