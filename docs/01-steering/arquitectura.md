# Steering — arquitectura

## Cuándo cargarlo

Componer una spec, NFR, estilo back/front, IAM, config, contenedores, ADR. Batería de pruebas a nivel sistema (pirámide): card `pruebas`.

## Cuándo no

Copy de un botón. Redactar EARS de una HU.

## Si aplica, cargar

1. `docs/03-architecture/catalog.md`
2. **Un** archivo de esa tabla (casi siempre `iam` y/o `c4` al componer identidad).
3. Un ADR concreto si ya existe la decisión. No leas `adr/` entero.
