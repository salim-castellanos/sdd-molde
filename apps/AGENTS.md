# AGENTS.md — apps

Artefactos generados. Cada subcarpeta puede ser un git submodule el día que duela.

No escribas código aquí sin spec `task-ready` y paso `implement` en el runbook activo.
El `AGENTS.md` de `api` / `web` / `mobile` / `infra` gana en stack.
